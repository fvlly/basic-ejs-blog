<%- include ('partials/header') %>


<h1>Home</h1>
<p> <%= homeContent %> </p>

 <% posts.forEach(post=>{ %>
     <h2><%= post.title %></h2>
     <p><em>Posted: <%= day %></em></p>

     <p><%= (post.content).substring(0,100) + "..." %> <a href="/posts/<%= post._id %>">Read More</a></p>
<% }) %>

<%- include ('partials/footer') %>